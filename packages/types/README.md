# @lasce/types

Pure TypeScript types shared across the workspace. No runtime code and no dependencies, so it can
be imported from the browser, the server, or a script without pulling anything along.

Types that need runtime validation live in [`@lasce/contracts`](../contracts) instead; types that
describe database rows are generated by [`@lasce/db`](../db).
